{
  "version": 2,
  "builds": [{
    "src": "./lib/server.js",
    "use": "@vercel/node"
  }],
  "routes": [{"handle": "filesystem"},
    {
      "src": "/lib/.*",
      "dest": "server.js"
    }
  ]
}
